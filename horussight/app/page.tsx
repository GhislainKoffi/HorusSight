// This page is the entry point of the Next.js app.
// It renders the main App component (client-side React SPA).
import App from '@/components/App';

export default function Page() {
  return <App />;
}
